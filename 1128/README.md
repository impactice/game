
# 언리얼 엔진 5 초보자 가이드

> 이 가이드는 언리얼 엔진 5를 처음 접하는 분들을 위한 단계별 실습 튜토리얼

---

## 목차

- [EP1: 언리얼 엔진 설치 및 실행](#ep1-언리얼-엔진-설치-및-실행)
- [EP2: 프로젝트 생성과 기본 가이드](#ep2-프로젝트-생성과-기본-가이드)
- [EP3: 블루프린트 기초](#ep3-블루프린트-기초)
- [EP4: C++ 개발 기초](#ep4-c-개발-기초)
- [EP5: 샘플 & 애셋 사용법](#ep5-샘플--애셋-사용법)

---

## EP1: 언리얼 엔진 설치 및 실행

### 1.1 Epic Games Launcher 설치

1. 웹 브라우저에서 **Epic Games**를 검색합니다
2. Epic Games 공식 홈페이지(https://www.epicgames.com)에 접속합니다
3. 상단 메뉴에서 **Download** 또는 **Download Launcher** 버튼을 클릭합니다
4. 다운로드된 설치 파일을 실행합니다

### 1.2 Epic Games 계정 생성

1. Epic Games Launcher 설치가 완료되면 실행합니다
2. **로그인** 화면에서 다음 중 하나를 선택합니다:
   - 새 계정 만들기
   - 기존 계정으로 로그인
   - Google, Facebook, Xbox 등의 계정과 연동
3. 계정 생성 시 필요한 정보를 입력합니다:
   - 이메일 주소
   - 비밀번호
   - 표시 이름
   - 이용 약관 동의

### 1.3 언리얼 엔진 설치

1. Epic Games Launcher 실행 (기본적으로 **News** 탭이 열립니다)
2. 좌측 메뉴에서 **Unreal Engine** 항목을 클릭합니다
3. 상단 메뉴에서 **Library** 탭을 선택합니다
4. 우측 상단의 노란색 **+ 엔진 버전** (또는 Install Engine) 버튼을 클릭합니다
5. 설치할 엔진 버전을 선택합니다 (최신 버전 권장: 5.x)

### 1.4 설치 옵션 설정

1. **설치(Install)** 버튼을 클릭하면 설치 경로 설정 화면이 나타납니다
2. **Options** 버튼을 클릭하여 설치 옵션을 선택합니다:
   - **Core Components** (필수): 기본 엔진 파일
   - **Starter Content**: 기본 애셋 및 샘플 콘텐츠
   - **Templates and Feature Packs**: 프로젝트 템플릿
   - **Engine Source** (선택): 엔진 소스 코드 (고급 사용자용)
   - **Target Platforms**: 
     - Windows
     - Mac (Mac에서만)
     - Linux
     - Android
     - iOS (Mac에서만)
3. 필요한 항목을 체크하고 **Apply** 버튼을 클릭합니다
4. 설치 경로를 확인하고 **Install** 버튼을 클릭합니다
5. 다운로드 및 설치가 완료될 때까지 기다립니다 (용량이 크므로 시간이 소요됩니다)

> **💡 TIP**: 설치 용량이 크므로(약 50GB 이상) 충분한 저장 공간을 확보해두세요.

### 1.5 기존 프로젝트 찾기

Library 탭을 아래로 스크롤하면 **My Projects** 섹션이 있습니다. 여기서 기존에 작성한 프로젝트를 확인하고 실행할 수 있습니다.

> **⚠️ 주의**: 언리얼 엔진의 기본 저장 위치가 아닌 다른 곳에 프로젝트를 저장한 경우 자동으로 인식되지 않을 수 있습니다.

---

## EP2: 프로젝트 생성과 기본 가이드

### 2.1 새 프로젝트 생성

1. Epic Games Launcher의 Library 탭에서 설치된 엔진 버전 옆의 **Launch** 버튼을 클릭합니다
2. **Unreal Project Browser** 창이 나타날 때까지 기다립니다
3. 좌측 카테고리에서 **GAMES**를 선택합니다
4. 프로젝트 템플릿을 선택합니다:
   - **Blank**: 빈 프로젝트 (완전히 처음부터 시작)
   - **First Person**: 1인칭 시점 템플릿
   - **Third Person**: 3인칭 시점 템플릿 ⭐ **(권장)**
   - **Top Down**: 상부 시점 템플릿
   - **Handheld AR**: AR(증강현실) 템플릿
   - **Vehicle**: 차량 게임 템플릿
   - **VR**: 가상현실 템플릿

5. **Third Person** 템플릿을 선택합니다

6. 프로젝트 설정을 구성합니다:
   - **Blueprint** 또는 **C++** 선택 (초보자는 Blueprint 권장)
   - **Target Platform**: Desktop / Mobile
   - **Quality Preset**: Maximum Quality / Scalable
   - **Starter Content**: With Starter Content 체크 권장
   - **Raytracing**: 선택사항 (고사양 그래픽 카드 필요)

7. 프로젝트 이름과 저장 위치를 설정합니다
8. **Create** 버튼을 클릭합니다
9. 프로젝트가 로딩될 때까지 기다립니다 (첫 실행 시 시간이 소요될 수 있습니다)

### 2.2 UI 레이아웃 설정

처음 실행하면 UE5의 새로운 레이아웃이 보일 수 있습니다. 익숙한 레이아웃으로 변경하려면:

1. 상단 메뉴에서 **Window** → **Load Layout**을 선택합니다
2. 다음 중 선택합니다:
   - **Default Editor Layout** (UE5 기본)
   - **Classic Layout** (UE4 스타일)

### 2.3 언리얼 엔진 화면 구성 이해하기

언리얼 엔진의 기본 인터페이스는 6개의 주요 영역으로 구성됩니다:

#### ① 뷰포트 (Viewport) - 중앙 큰 화면
- 게임 세계를 실시간으로 볼 수 있는 3D 공간
- 오브젝트 배치 및 레벨 디자인을 수행하는 주 작업 공간
- 게임 실행 시 플레이 화면으로 전환됨

#### ② 메뉴바 & 툴바 - 최상단
- **File**: 프로젝트 저장, 불러오기 등
- **Edit**: 편집 관련 기능
- **Window**: 창 레이아웃 설정
- **Play, Build, Source Control** 등의 빠른 실행 버튼

#### ③ 아웃라이너 (Outliner) - 우측 상단
- 레벨에 배치된 모든 액터(오브젝트)의 계층 구조 표시
- Unity의 Hierarchy와 동일한 역할
- 액터 검색, 선택, 그룹화 가능
- 가시성 및 잠금 토글 기능

#### ④ 디테일 (Details) - 우측 하단
- 선택한 액터의 모든 속성을 표시하고 편집
- Transform(위치, 회전, 크기), 머티리얼, 컴포넌트 등
- Unity의 Inspector와 유사

#### ⑤ 콘텐츠 브라우저 (Content Browser) - 하단
- 프로젝트의 모든 애셋 관리
- 메시, 텍스처, 머티리얼, 블루프린트, 사운드 등
- 폴더 구조로 정리되며 애셋 임포트/생성 가능
- 단축키: **Ctrl + Space**로 빠르게 열고 닫기

#### ⑥ 액터 배치 (Place Actors) - 좌측
- 레벨에 배치 가능한 기본 액터 목록
- Lights, Cameras, Geometry(기본 도형), Volumes 등
- 드래그 앤 드롭으로 뷰포트에 배치

> **💡 TIP**: 각 패널은 드래그하여 위치를 변경하거나 도킹/해제할 수 있습니다. Window 메뉴에서 원하는 패널을 켜고 끌 수 있습니다.

### 2.4 기본 조작법 익히기

#### 뷰포트 카메라 조작

| 조작 | 방법 |
|------|------|
| 자유 시점 이동 | **마우스 우클릭** 누른 채 **W/A/S/D** 키 |
| 전진/후진 | W / S |
| 좌/우 이동 | A / D |
| 상승/하강 | E / Q |
| 회전 | 우클릭 + 마우스 이동 |
| 패닝 (화면 이동) | **마우스 중간 버튼** 누른 채 드래그 |
| 줌 인/아웃 | **마우스 휠** 스크롤 |
| 선택한 오브젝트에 포커스 | 오브젝트 선택 후 **F** 키 |
| 오브젝트 회전하며 보기 | **Alt + 좌클릭** 드래그 |

#### 오브젝트 조작

1. **뷰포트에서 오브젝트 선택**: 좌클릭
2. **기즈모 모드 전환** 단축키:
   - **Q**: 선택 모드 (기즈모 숨김)
   - **W**: 이동(Move) 모드
   - **E**: 회전(Rotate) 모드
   - **R**: 크기(Scale) 모드

3. **기즈모 사용법**:
   - **빨간색 화살표/원/큐브**: X축 조작
   - **초록색 화살표/원/큐브**: Y축 조작
   - **파란색 화살표/원/큐브**: Z축 조작
   - 화살표를 드래그하여 오브젝트 이동/회전/크기 조절

#### 스냅 (Snap) 설정

뷰포트 상단의 툴바에서:

1. **스냅 On/Off**: 
   - 위치 스냅 토글
   - 회전 스냅 토글
   - 크기 스냅 토글

2. **스냅 크기 조정**:
   - 위치 스냅: 10, 50, 100 단위 등
   - 회전 스냅: 5°, 10°, 45°, 90° 등
   - 크기 스냅: 0.25, 0.5, 1.0 배율 등

3. **카메라 속도 조절**:
   - 우측 상단의 속도 슬라이더로 WASD 이동 속도 조절
   - 빠른 이동이 필요할 때 증가, 정밀 작업 시 감소

> **💡 TIP**: 정밀한 작업이 필요할 때는 스냅을 끄고 작업하세요.

### 2.5 게임 실행해보기

1. 상단 툴바의 **Play** (▶) 버튼을 클릭합니다
2. 뷰포트가 게임 모드로 전환됩니다
3. **마우스**와 **W/A/S/D** 키로 캐릭터를 조작합니다
4. **스페이스바**로 점프합니다
5. **ESC** 키 또는 **Stop** (■) 버튼으로 게임을 종료합니다

> **💡 TIP**: Play 버튼 옆의 드롭다운으로 다양한 실행 옵션을 선택할 수 있습니다:
> - **Selected Viewport**: 현재 뷰포트에서 실행
> - **New Editor Window**: 새 창에서 실행
> - **Standalone Game**: 독립 실행형 창
> - **Simulate**: 물리 시뮬레이션만 실행

### 2.6 새 레벨 생성하기

레벨은 게임의 한 장면이나 스테이지를 의미합니다.

1. **콘텐츠 브라우저**에서 **Content** 폴더로 이동합니다
2. 빈 공간에서 **우클릭**합니다
3. **New Folder**를 선택하여 새 폴더를 만듭니다 (예: "MyLevels")
4. 생성한 폴더에 들어갑니다
5. 다시 **우클릭** → **Level**을 선택합니다
6. 레벨 이름을 입력합니다 (예: "TestLevel")
7. 생성된 레벨을 **더블클릭**하여 엽니다
8. 검은색 빈 화면(새 레벨)이 나타납니다

### 2.7 레벨에 환경 추가하기

새로 만든 레벨은 완전히 비어있어 아무것도 보이지 않습니다. 기본 조명과 하늘을 추가해봅시다.

#### 방법 1: 환경 라이트 믹서 사용 (권장)

1. 상단 메뉴에서 **Window** → **Environment Light Mixer**를 선택합니다
2. **Create Light** 섹션에서 다음을 클릭:
   - **Create Directional Light**: 태양광 생성
   - **Create Sky Light**: 간접광 생성
   - **Create Sky Atmosphere**: 하늘 대기 효과
   - **Create Volumetric Cloud**: 구름 추가
   - **Create Height Fog**: 안개 효과

3. 모든 요소가 자동으로 레벨에 추가됩니다

#### 방법 2: 수동으로 액터 배치

1. 좌측의 **Place Actors** 패널을 엽니다
2. **Lights** 카테고리를 펼칩니다
3. 다음 액터들을 뷰포트로 드래그하여 배치합니다:
   - **Directional Light**: 태양광 (메인 광원)
   - **Sky Light**: 전역 조명 (주변광)

4. **Visual Effects** 카테고리에서:
   - **Sky Atmosphere**: 하늘 효과
   - **Volumetric Cloud**: 구름
   - **Exponential Height Fog**: 안개

#### 각 요소의 역할

| 액터 | 역할 |
|------|------|
| **Directional Light** | 태양광 시뮬레이션, 무한히 먼 광원에서 오는 평행광 |
| **Sky Light** | 하늘에서 오는 간접광, 실내/그림자 영역을 자연스럽게 밝힘 |
| **Sky Atmosphere** | 지구와 유사한 대기 효과, 하늘 색상과 원근감 |
| **Volumetric Cloud** | 실시간 구름 렌더링 |
| **Exponential Height Fog** | 고도에 따른 안개, 거리감과 깊이감 표현 |

### 2.8 태양 방향 조절하기

1. 뷰포트에서 **Directional Light**를 선택합니다 (아웃라이너에서 선택해도 됨)
2. **E** 키를 눌러 회전 모드로 전환합니다
3. 기즈모를 드래그하여 태양 방향을 변경합니다
4. 시간대가 바뀌는 것을 확인할 수 있습니다 (낮/석양/밤)

### 2.9 레벨 꾸미기 실습

Place Actors 패널의 **Basic** 카테고리에서 다양한 도형을 배치해봅시다:

1. **Cube**: 육면체
2. **Sphere**: 구체
3. **Cylinder**: 원기둥
4. **Cone**: 원뿔
5. **Plane**: 평면 (바닥으로 사용 가능)

**실습 과제**: 
- Plane을 바닥으로 배치
- 다양한 도형들을 조합하여 간단한 건물이나 장애물 만들기
- 위치(W), 회전(E), 크기(R) 조작 연습하기

> **💡 TIP**: 여러 오브젝트를 선택하려면 Ctrl 키를 누른 채 클릭하거나, 드래그로 박스 선택을 사용하세요.

### 2.10 레벨 저장하기

1. **Ctrl + S** 단축키 또는
2. 상단 메뉴 **File** → **Save Current Level**
3. 프로젝트 전체 저장: **File** → **Save All**

---

## EP3: 블루프린트 기초

블루프린트는 언리얼 엔진의 비주얼 스크립팅 시스템입니다. 코드를 작성하지 않고도 게임 로직을 만들 수 있습니다.

### 3.1 블루프린트의 종류

#### 1) 레벨 블루프린트 (Level Blueprint)
- 특정 레벨에서만 동작하는 스크립트
- 해당 레벨에 종속적
- 문 열기, 컷씬 트리거 등 레벨별 이벤트에 적합

#### 2) 블루프린트 클래스 (Blueprint Class)
- 재사용 가능한 독립적인 액터
- 여러 레벨에서 사용 가능
- 캐릭터, 아이템, 적 등을 만들 때 사용

### 3.2 레벨 블루프린트 열기

1. 상단 툴바에서 **Blueprints** 아이콘을 클릭합니다
2. 드롭다운에서 **Open Level Blueprint**를 선택합니다
3. 새 창에 블루프린트 에디터가 열립니다

### 3.3 블루프린트 클래스 생성하기

1. **콘텐츠 브라우저**에서 블루프린트를 저장할 폴더로 이동합니다
2. 빈 공간에서 **우클릭**합니다
3. **Blueprint Class**를 선택합니다
4. **Common Classes**에서 **Actor**를 선택합니다
   - **Actor**: 가장 기본적인 배치 가능한 오브젝트
   - **Pawn**: 플레이어/AI가 조종할 수 있는 오브젝트
   - **Character**: 캐릭터 전용 (이동, 점프 등 기본 기능 포함)
5. 블루프린트 이름을 입력합니다 (예: "BP_MyFirstActor")
   > **💡 TIP**: BP_ 접두사를 붙이면 블루프린트임을 쉽게 알 수 있습니다
6. 생성된 블루프린트를 **더블클릭**하여 엽니다

### 3.4 블루프린트 에디터 화면 구성

블루프린트 에디터는 다음 영역으로 구성됩니다:

#### ① Components (좌측 상단)
- 액터를 구성하는 컴포넌트 목록
- Static Mesh, Camera, Light 등 추가 가능
- 계층 구조로 관리

#### ② My Blueprint (좌측)
- 변수(Variables)
- 함수(Functions)
- 매크로(Macros)
- 이벤트 그래프(Event Graphs)
- 등을 관리하는 패널

#### ③ Viewport (중앙 상단)
- 액터의 비주얼을 확인하는 3D 뷰
- 컴포넌트 배치 및 미리보기

#### ④ Event Graph (중앙)
- 실제 스크립트 로직을 작성하는 공간
- 노드 기반 프로그래밍

#### ⑤ Details (우측)
- 선택한 컴포넌트나 노드의 세부 설정
- 속성 편집

### 3.5 기본 실습: 키 입력으로 메시지 출력하기

키보드 입력을 받아 화면에 텍스트를 출력하는 간단한 블루프린트를 만들어봅시다.

#### Step 1: Enhanced Input Action 생성

언리얼 엔진 5는 Enhanced Input 시스템을 사용합니다.

1. **콘텐츠 브라우저**에서 프로젝트의 **Input** 폴더로 이동합니다
   - 경로: `Content/ThirdPerson/Input/`
   - 폴더가 없다면 생성하세요

2. 빈 공간에서 **우클릭** → **Input** → **Input Action**을 선택합니다
3. 이름을 입력합니다: `IA_TestKey`
   > **💡 TIP**: IA_ 접두사는 Input Action을 의미합니다

#### Step 2: Input Mapping Context에 추가

1. **콘텐츠 브라우저**에서 `Content/ThirdPerson/Input/` 폴더의 **IMC_Default**를 더블클릭하여 엽니다
2. **Mappings** 섹션에서 **+ 버튼**을 클릭하여 새 매핑을 추가합니다
3. 방금 만든 **IA_TestKey**를 선택합니다
4. 매핑 항목을 펼치고 **+ 버튼**을 클릭하여 키를 추가합니다
5. 키 입력 칸을 클릭하고 원하는 키를 누릅니다 (예: **T** 키)
6. **저장**합니다

#### Step 3: 레벨 블루프린트에서 스크립트 작성

1. **Blueprints** → **Open Level Blueprint**를 엽니다
2. Event Graph의 빈 공간에서 **우클릭**합니다
3. 검색창에 **IA_TestKey**를 입력하고 선택합니다
   - 또는 **Input** → **Enhanced Input Actions** → **IA_TestKey** 선택
4. 노드가 생성되면 **Triggered** 핀에서 **드래그**합니다
5. 검색창에 **Print String**을 입력하고 선택합니다
6. **Print String** 노드의 **In String** 에 원하는 메시지를 입력합니다
   - 예: "Hello, Unreal Engine!"
7. 필요시 **Duration**(표시 시간)과 **Text Color**를 설정합니다

**최종 연결 구조**:
```
IA_TestKey (Triggered) → Print String
```

#### Step 4: 테스트

1. 블루프린트를 **컴파일** (좌상단의 Compile 버튼)합니다
2. **저장**합니다
3. 블루프린트 창을 닫고 메인 에디터로 돌아갑니다
4. **Play** 버튼으로 게임을 실행합니다
5. 설정한 키(**T**)를 눌러봅니다
6. 화면 좌상단에 메시지가 나타나는지 확인합니다

> **⚠️ 문제 해결**: 메시지가 나타나지 않는다면:
> - Input Mapping Context가 올바르게 설정되었는지 확인
> - 블루프린트를 컴파일하고 저장했는지 확인
> - 레벨 블루프린트에 스크립트를 작성했는지 확인

### 3.6 고급 실습: Third Person 캐릭터 이동 분석

Third Person 템플릿의 캐릭터 이동 시스템을 분석해봅시다.

#### 캐릭터 블루프린트 열기

1. 콘텐츠 브라우저에서 `Content/ThirdPerson/Blueprints/` 폴더로 이동합니다
2. **BP_ThirdPersonCharacter**를 더블클릭하여 엽니다

#### Event Graph 주요 로직 분석

**Event BeginPlay** (게임 시작 시 1회 실행):
```
Event BeginPlay
→ Get Player Controller (플레이어 컨트롤러 가져오기)
→ Cast to Player Controller (타입 변환)
→ Get Enhanced Input Local Player Subsystem (입력 서브시스템 가져오기)
→ Add Mapping Context (IMC_Default를 플레이어에 연결)
```
- 이 로직은 키보드/마우스 입력을 캐릭터와 연결합니다

**IA_Move** (이동 입력):
```
IA_Move (Triggered)
→ Get Control Rotation (현재 카메라 방향 가져오기)
→ Break Rotation (X, Y, Z 회전값 분리)
→ Make Rotation (Yaw만 사용하여 새 회전값 생성)
→ Get Forward Vector (앞 방향 벡터)
→ Get Right Vector (오른쪽 방향 벡터)
→ Add Movement Input (입력값만큼 이동)
```
- W/S: 앞/뒤 이동 (Forward Vector)
- A/D: 좌/우 이동 (Right Vector)

**IA_Look** (카메라 회전):
```
IA_Look (Triggered)
→ Add Controller Yaw Input (좌우 회전)
→ Add Controller Pitch Input (상하 회전)
```

**IA_Jump** (점프):
```
IA_Jump (Started) → Jump
IA_Jump (Completed) → Stop Jumping
```

### 3.7 응용 실습: 움직이는 큐브 만들기

새 프로젝트에서 직접 움직이는 오브젝트를 만들어봅시다.

1. **빈 프로젝트** 또는 **Blank** 템플릿으로 새 프로젝트를 시작합니다
2. 블루프린트 클래스 생성: **Pawn** 선택 (이름: BP_MovingCube)
3. Pawn 블루프린트를 열어 다음 작업을 수행합니다:

#### Components 설정

1. **Components** 패널에서 **Add** 버튼 클릭
2. **Static Mesh**를 추가하고 이름을 "Body"로 변경
3. **Details** 패널에서 Static Mesh → **Cube** 선택
4. **Add** → **Camera**를 추가합니다
5. Camera를 선택하고 위치를 조정합니다 (예: Z: 200, Y: -300)

#### 입력 설정 (Enhanced Input 사용)

1. `Content/Input/` 폴더 생성
2. Input Action 4개 생성:
   - **IA_MoveForward**
   - **IA_MoveRight**
3. Input Mapping Context 생성: **IMC_PlayerInput**
4. IMC_PlayerInput을 열어 매핑 추가:
   - IA_MoveForward: W (값: 1.0), S (값: -1.0)
   - IA_MoveRight: D (값: 1.0), A (값: -1.0)

#### Event Graph 스크립트 작성

**BeginPlay 로직**:
```
Event BeginPlay
→ Get Player Controller
→ Cast to PlayerController
→ Get Enhanced Input Local Player Subsystem
→ Add Mapping Context (IMC_PlayerInput, Priority: 0)
```

**IA_MoveForward 로직**:
```
IA_MoveForward (Triggered)
→ Get Action Value (float 값)
→ Get Actor Forward Vector
→ Multiply (Forward Vector * Input Value)
→ Add Movement Input
```

**IA_MoveRight 로직**:
```
IA_MoveRight (Triggered)
→ Get Action Value (float 값)
→ Get Actor Right Vector
→ Multiply (Right Vector * Input Value)
→ Add Movement Input
```

#### Floating Pawn Movement 추가

1. **Components** 패널에서 **Add** 클릭
2. **Floating Pawn Movement**를 추가합니다
3. Details에서 이동 속도 등을 조정할 수 있습니다

#### 레벨에 배치 및 테스트

1. BP_MovingCube를 뷰포트에 드래그하여 배치합니다
2. 배치된 인스턴스를 선택합니다
3. **Details** 패널에서:
   - **Pawn** 섹션 → **Auto Possess Player**: **Player 0**으로 설정
   - **AI Controller Class**: **None**
4. **Play**를 눌러 W/A/S/D로 큐브를 조종해봅니다

---

## EP4: C++ 개발 기초

언리얼 엔진은 C++로도 프로그래밍할 수 있습니다. 더 높은 성능과 저수준 제어가 필요할 때 사용합니다.

### 4.1 Visual Studio 설치

#### Visual Studio 다운로드

1. https://visualstudio.microsoft.com 접속
2. **Visual Studio Community** (무료) 다운로드
3. 설치 프로그램 실행

#### 워크로드 설치

Visual Studio Installer에서 다음을 선택합니다:

1. **C++를 사용한 게임 개발** (Game development with C++) ✅ 필수
2. 우측의 옵션 확인:
   - ✅ C++ 프로파일링 도구
   - ✅ Windows 10 SDK 또는 Windows 11 SDK
   - ✅ Unreal Engine 설치 관리자

3. **개별 구성 요소** 탭에서 추가로 선택:
   - ✅ .NET Framework 4.6.2 SDK
   - ✅ Windows Universal CRT SDK

4. **설치** 버튼 클릭

> **💡 TIP**: 설치 용량이 크므로(약 20GB 이상) 시간이 걸립니다.

### 4.2 언리얼 엔진과 Visual Studio 연동

#### 첫 연결

1. 언리얼 엔진 프로젝트를 엽니다
2. 상단 메뉴에서 **Tools** → **Generate Visual Studio Project Files**를 실행합니다
3. 프로젝트 폴더에 `.sln` 파일이 생성됩니다
4. 상단 메뉴에서 **Tools** → **Open Visual Studio**를 선택합니다

#### 추가 구성 요소 설치

Visual Studio가 처음 열릴 때 추가 구성 요소를 요구할 수 있습니다:

1. **솔루션 탐색기** (우측)에서 설치 알림이 나타나면 **설치** 클릭
2. 다음 구성 요소가 설치됩니다:
   - **.NET Desktop Development**: Windows API 및 COM 사용
   - **.NET Framework 런타임**: 언리얼 빌드 시스템에 필요

#### 주요 설정

1. Visual Studio에서 상단의 **솔루션 구성**을 **Development Editor**로 설정합니다
2. **솔루션 플랫폼**을 **Win64**로 설정합니다

### 4.3 문제 해결: 링크 에러

#### 상황: Visual Studio를 재설치한 후 빌드 크래시

**오류 메시지 예시**:
```
LINK : fatal error LNK1181: 
'C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Tools\MSVC\14.38.33130\lib\x64\legacy_stdio_wide_specifiers.lib' 
파일을 열 수 없습니다.
```

**원인**: 
- 언리얼 프로젝트가 이전 Visual Studio 버전 경로를 기억하고 있음
- 프로젝트 캐시가 남아있음

**해결 방법**:

1. 언리얼 에디터와 Visual Studio를 **모두 종료**합니다
2. 프로젝트 폴더에서 다음 폴더/파일을 **삭제**:
   - **Binaries** 폴더
   - **Intermediate** 폴더
   - **Saved** 폴더
   - **.vs** 폴더 (숨김 폴더)
   - `프로젝트이름.sln` 파일

3. 프로젝트 폴더의 `.uproject` 파일을 **우클릭** → **Generate Visual Studio project files**

4. 생성된 `.sln` 파일을 열어 Visual Studio 실행

5. Visual Studio에서 **빌드** → **솔루션 다시 빌드**

### 4.4 C++ 액터 클래스 생성하기

#### 새 C++ 클래스 추가

1. 언리얼 에디터에서 **Tools** → **New C++ Class**를 선택합니다
2. 부모 클래스 선택 화면에서 **Actor**를 선택합니다
   - Actor: 기본 배치 가능한 오브젝트
   - Pawn: 컨트롤 가능
   - Character: 캐릭터 전용
3. **Next** 클릭
4. 클래스 이름 입력: `MyActor`
5. **Public** 또는 **Private** 선택:
   - **Public**: 다른 모듈에서도 접근 가능
   - **Private**: 현재 프로젝트 내부에서만 사용
6. **Create Class** 클릭
7. Visual Studio가 자동으로 열리고 컴파일이 시작됩니다

### 4.5 C++ 코드 구조 이해하기

#### MyActor.h (헤더 파일)

```cpp
#pragma once

#include "CoreMinimal.h"
#include "GameFramework/Actor.h"
#include "MyActor.generated.h"

UCLASS()
class YOURPROJECT_API AMyActor : public AActor
{
    GENERATED_BODY()
    
public:    
    AMyActor();

protected:
    virtual void BeginPlay() override;

public:    
    virtual void Tick(float DeltaTime) override;
};
```

**주요 요소 설명**:

| 코드 | 설명 |
|------|------|
| `#include "CoreMinimal.h"` | 언리얼 엔진의 핵심 기능 (로그, 수학, 타입 정의 등) |
| `#include "GameFramework/Actor.h"` | Actor 클래스의 정의 |
| `#include "MyActor.generated.h"` | 언리얼 리플렉션 시스템이 자동 생성하는 헤더 (항상 마지막에 위치) |
| `UCLASS()` | 이 클래스가 언리얼 오브젝트임을 선언하는 매크로 |
| `GENERATED_BODY()` | 언리얼이 필요한 코드를 자동으로 생성하는 매크로 |
| `AMyActor` | Actor 클래스는 항상 **A** 접두사 사용 |
| `BeginPlay()` | 액터가 생성되고 게임이 시작될 때 1회 호출 |
| `Tick()` | 매 프레임마다 호출 (Unity의 Update()와 동일) |

#### MyActor.cpp (소스 파일)

```cpp
#include "MyActor.h"

AMyActor::AMyActor()
{
    PrimaryActorTick.bCanEverTick = true;
}

void AMyActor::BeginPlay()
{
    Super::BeginPlay();
}

void AMyActor::Tick(float DeltaTime)
{
    Super::Tick(DeltaTime);
}
```

### 4.6 실습: 화면에 메시지 출력하기

#### 코드 작성

**MyActor.h**에 함수 선언 추가:

```cpp
#pragma once

#include "CoreMinimal.h"
#include "GameFramework/Actor.h"
#include "MyActor.generated.h"

UCLASS()
class YOURPROJECT_API AMyActor : public AActor
{
    GENERATED_BODY()
    
public:    
    AMyActor();

protected:
    virtual void BeginPlay() override;
    
    // 새로 추가하는 함수
    void PrintMessage();

public:    
    virtual void Tick(float DeltaTime) override;
};
```

**MyActor.cpp**에 함수 구현 추가:

```cpp
#include "MyActor.h"

AMyActor::AMyActor()
{
    PrimaryActorTick.bCanEverTick = true;
}

void AMyActor::BeginPlay()
{
    Super::BeginPlay();
    
    // BeginPlay에서 메시지 출력 함수 호출
    PrintMessage();
}

void AMyActor::PrintMessage()
{
    // 화면에 텍스트 출력
    if (GEngine)
    {
        GEngine->AddOnScreenDebugMessage(
            -1,                    // 키 (-1 = 고유 키 자동 생성)
            5.0f,                  // 표시 시간 (초)
            FColor::Green,         // 텍스트 색상
            TEXT("Hello from C++!") // 표시할 메시지
        );
    }
    
    // 로그 출력 (Output Log에서 확인 가능)
    UE_LOG(LogTemp, Warning, TEXT("MyActor has been created!"));
}

void AMyActor::Tick(float DeltaTime)
{
    Super::Tick(DeltaTime);
}
```

#### 컴파일 및 사용

1. Visual Studio에서 **Ctrl + Shift + B**로 빌드합니다
2. 또는 상단 메뉴 **빌드** → **솔루션 빌드**
3. 빌드 성공 메시지를 확인합니다
4. 언리얼 에디터로 돌아갑니다 (자동으로 핫 리로드됨)

#### 블루프린트로 변환

C++ 클래스를 그대로 사용할 수도 있지만, 블루프린트로 변환하면 에디터에서 쉽게 편집할 수 있습니다:

1. **콘텐츠 브라우저**에서 **C++ Classes** 폴더를 펼칩니다
2. 프로젝트 이름 폴더를 펼칩니다
3. **MyActor** 클래스를 찾아 **우클릭**합니다
4. **Create Blueprint class based on MyActor**를 선택합니다
5. 이름을 입력합니다 (예: `BP_MyActor`)
6. 생성된 블루프린트를 뷰포트에 드래그하여 배치합니다
7. **Play**를 눌러 메시지가 나타나는지 확인합니다

### 4.7 고급 실습: C++로 이동 가능한 Pawn 만들기

더 복잡한 예제로, 키보드 입력을 받아 이동하는 Pawn을 C++로 만들어봅시다.

#### Step 1: 새 C++ Pawn 클래스 생성

1. **Tools** → **New C++ Class** → **Pawn** 선택
2. 이름: `MovablePawn`
3. **Create Class**

#### Step 2: 헤더 파일 작성 (MovablePawn.h)

```cpp
#pragma once

#include "CoreMinimal.h"
#include "GameFramework/Pawn.h"
#include "InputActionValue.h"
#include "MovablePawn.generated.h"

UCLASS()
class YOURPROJECT_API AMovablePawn : public APawn
{
    GENERATED_BODY()

public:
    AMovablePawn();

protected:
    virtual void BeginPlay() override;

public:    
    virtual void Tick(float DeltaTime) override;
    virtual void SetupPlayerInputComponent(class UInputComponent* PlayerInputComponent) override;

    // Components
    UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Components")
    class UStaticMeshComponent* MeshComponent;

    UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Components")
    class UFloatingPawnMovement* MovementComponent;

    UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Components")
    class USpringArmComponent* SpringArm;

    UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Components")
    class UCameraComponent* Camera;

    // Input
    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Input")
    class UInputMappingContext* InputMappingContext;

    UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Input")
    class UInputAction* MoveAction;

protected:
    // Input Functions
    void Move(const FInputActionValue& Value);
};
```

**코드 설명**:

- `UPROPERTY()`: 언리얼이 이 변수를 관리하도록 하는 매크로
  - `VisibleAnywhere`: 에디터에서 볼 수 있지만 편집 불가
  - `EditAnywhere`: 에디터에서 편집 가능
  - `BlueprintReadOnly`: 블루프린트에서 읽기만 가능
- `UStaticMeshComponent`: 3D 메시 컴포넌트
- `UFloatingPawnMovement`: 물리 없는 부드러운 이동
- `USpringArmComponent`: 카메라 붐 (거리 유지)
- `UCameraComponent`: 카메라
- `UInputMappingContext`: Enhanced Input 컨텍스트
- `UInputAction`: 입력 액션

#### Step 3: 소스 파일 작성 (MovablePawn.cpp)

```cpp
#include "MovablePawn.h"
#include "Components/StaticMeshComponent.h"
#include "GameFramework/FloatingPawnMovement.h"
#include "GameFramework/SpringArmComponent.h"
#include "Camera/CameraComponent.h"
#include "EnhancedInputComponent.h"
#include "EnhancedInputSubsystems.h"
#include "InputActionValue.h"

AMovablePawn::AMovablePawn()
{
    PrimaryActorTick.bCanEverTick = true;

    // Create Root Component (Mesh)
    MeshComponent = CreateDefaultSubobject<UStaticMeshComponent>(TEXT("MeshComponent"));
    RootComponent = MeshComponent;

    // Create Movement Component
    MovementComponent = CreateDefaultSubobject<UFloatingPawnMovement>(TEXT("MovementComponent"));

    // Create Spring Arm
    SpringArm = CreateDefaultSubobject<USpringArmComponent>(TEXT("SpringArm"));
    SpringArm->SetupAttachment(RootComponent);
    SpringArm->TargetArmLength = 400.0f;
    SpringArm->SetRelativeRotation(FRotator(-30.0f, 0.0f, 0.0f));

    // Create Camera
    Camera = CreateDefaultSubobject<UCameraComponent>(TEXT("Camera"));
    Camera->SetupAttachment(SpringArm);
}

void AMovablePawn::BeginPlay()
{
    Super::BeginPlay();
    
    // Add Input Mapping Context
    if (APlayerController* PlayerController = Cast<APlayerController>(Controller))
    {
        if (UEnhancedInputLocalPlayerSubsystem* Subsystem = 
            ULocalPlayer::GetSubsystem<UEnhancedInputLocalPlayerSubsystem>(PlayerController->GetLocalPlayer()))
        {
            Subsystem->AddMappingContext(InputMappingContext, 0);
        }
    }
}

void AMovablePawn::Tick(float DeltaTime)
{
    Super::Tick(DeltaTime);
}

void AMovablePawn::SetupPlayerInputComponent(UInputComponent* PlayerInputComponent)
{
    Super::SetupPlayerInputComponent(PlayerInputComponent);

    // Bind Enhanced Input Actions
    if (UEnhancedInputComponent* EnhancedInputComponent = Cast<UEnhancedInputComponent>(PlayerInputComponent))
    {
        EnhancedInputComponent->BindAction(MoveAction, ETriggerEvent::Triggered, this, &AMovablePawn::Move);
    }
}

void AMovablePawn::Move(const FInputActionValue& Value)
{
    // Get movement input (2D vector)
    FVector2D MovementVector = Value.Get<FVector2D>();

    if (Controller != nullptr)
    {
        // Get forward and right vectors
        const FRotator Rotation = Controller->GetControlRotation();
        const FRotator YawRotation(0, Rotation.Yaw, 0);

        const FVector ForwardDirection = FRotationMatrix(YawRotation).GetUnitAxis(EAxis::X);
        const FVector RightDirection = FRotationMatrix(YawRotation).GetUnitAxis(EAxis::Y);

        // Add movement
        AddMovementInput(ForwardDirection, MovementVector.Y);
        AddMovementInput(RightDirection, MovementVector.X);
    }
}
```

#### Step 4: 컴파일 및 블루프린트 생성

1. Visual Studio에서 빌드합니다 (**Ctrl + Shift + B**)
2. 언리얼 에디터로 돌아갑니다
3. **C++ Classes** 폴더에서 **MovablePawn** 우클릭
4. **Create Blueprint class based on MovablePawn** 선택
5. 이름: `BP_MovablePawn`

#### Step 5: 블루프린트 설정

1. **BP_MovablePawn**을 더블클릭하여 엽니다
2. **Components** 패널에서:
   - **MeshComponent** 선택
   - **Static Mesh** → **Cube** 또는 원하는 메시 선택
   - **Material** 설정 (선택사항)
3. **Class Defaults** (좌측 상단 버튼) 클릭
4. **Input** 섹션에서:
   - **Input Mapping Context** → 기존에 만든 IMC 또는 새로 생성
   - **Move Action** → 기존에 만든 IA 또는 새로 생성

#### Step 6: Input Action 생성 (필요시)

1. `Content/Input/` 폴더에서 **우클릭** → **Input** → **Input Action**
2. 이름: `IA_Move`
3. **IA_Move**를 열어 **Value Type**: **Axis2D (Vector2D)** 설정
4. **IMC_PlayerInput**을 열어:
   - **IA_Move** 추가
   - W: (0, 1)
   - S: (0, -1)
   - A: (-1, 0)
   - D: (1, 0)
   - 또는 Keyboard → **W, A, S, D Keys** 선택하고 2D Axis로 설정

#### Step 7: 레벨에 배치 및 설정

1. **BP_MovablePawn**을 뷰포트에 드래그합니다
2. 배치된 인스턴스 선택
3. **Details** 패널:
   - **Pawn** → **Auto Possess Player**: **Player 0**
   - **AI Controller Class**: **None**
4. **Play**로 테스트합니다

---

## EP5: 샘플 & 애셋 사용법

언리얼 엔진은 다양한 무료 샘플 프로젝트와 애셋을 제공합니다.

### 5.1 샘플 프로젝트 다운로드

#### 방법 1: Epic Games Launcher에서

1. **Epic Games Launcher**를 실행합니다
2. 상단 메뉴에서 **Unreal Engine** → **Samples** 탭을 선택합니다
3. 다양한 샘플 프로젝트를 확인할 수 있습니다:
   - **Content Examples**: 기능별 예제 모음 ⭐ 강력 추천
   - **Action RPG**: 액션 RPG 샘플
   - **Lyra Starter Game**: 현대적 멀티플레이어 게임 프레임워크
   - **Vehicle Template**: 차량 게임 템플릿
   - **Shooter Game**: FPS 샘플
4. 원하는 샘플을 선택하고 **내 라이브러리 추가** 또는 **무료 받기** 클릭
5. **Library** 탭으로 이동하여 **내 프로젝트** 섹션에서 해당 샘플 찾기
6. **프로젝트 생성** 버튼을 클릭합니다
7. 엔진 버전 선택 (최신 버전 권장)
8. 프로젝트 이름과 저장 위치 설정
9. **Create** 클릭

#### Content Examples 특별 추천

**Content Examples**는 언리얼 엔진의 거의 모든 기능을 예제로 보여주는 프로젝트입니다:

- **Audio**: 사운드 시스템
- **Blueprint_Communication**: 블루프린트 간 통신
- **Decals**: 데칼 (총탄 자국, 스티커 등)
- **Lighting**: 조명 시스템
- **Materials**: 머티리얼 제작
- **Math_Hall**: 수학 함수 예제
- **Networking**: 멀티플레이어
- **Niagara**: 파티클 시스템
- **Physics**: 물리 시뮬레이션
- **PostProcessing**: 후처리 효과
- **Reflections**: 반사 효과
- **Landscapes**: 지형 제작

### 5.2 Fab (구 Marketplace)에서 애셋 다운로드

Fab은 무료/유료 애셋을 제공하는 마켓플레이스입니다.

#### 무료 애셋 찾기

1. **Epic Games Launcher** 실행
2. 좌측 메뉴에서 **Fab** 항목 클릭
3. 상단 카테고리에서 **모든 애셋** 선택
4. 좌측 필터 패널:
   - **가격** → **무료** 체크
   - **카테고리** → 원하는 카테고리 선택 (예: Environments, Props, Characters)
   - **엔진 버전** → UE5 호환 확인
5. 원하는 애셋을 클릭합니다
6. 상세 페이지에서 **무료** 또는 **카트에 추가** 클릭
7. **구독** 또는 **체크아웃** 버튼 클릭

#### 추천 무료 애셋

- **Paragon 캐릭터**: Epic의 MOBA 게임에서 사용된 고품질 캐릭터
- **Infinity Blade**: 모바일 게임의 무기와 환경
- **Starter Content**: 기본 애셋 팩
- **Landscape Mountains**: 산악 지형
- **Stylized Forest**: 스타일화된 숲

#### 프로젝트에 애셋 추가

1. **Library** 탭으로 이동
2. **내 라이브러리** 또는 **Vault** 섹션에서 다운로드한 애셋 찾기
3. **프로젝트에 추가** 버튼 클릭
4. 추가할 프로젝트 선택 (현재 열려있는 프로젝트 또는 다른 프로젝트)
5. **프로젝트에 추가** 확인
6. 언리얼 에디터의 콘텐츠 브라우저에서 새 폴더로 애셋이 추가됨

> **💡 TIP**: 애셋 이름으로 된 폴더가 콘텐츠 브라우저에 생성됩니다. 예: `Content/InfinityBladeGrassLands/`

### 5.3 Quixel Bridge (Megascans)

Quixel Megascans는 실사 스캔 애셋 라이브러리입니다. Epic Games 계정이 있으면 무료입니다.

#### Quixel Bridge 설치

1. Epic Games Launcher에서 **Fab** → **Quixel**
2. 또는 https://quixel.com/bridge 방문
3. **Bridge 다운로드** 클릭
4. 설치 후 Epic Games 계정으로 로그인

#### 애셋 다운로드

1. Bridge 앱 실행
2. 카테고리 선택:
   - **3D Assets**: 바위, 나무, 식물 등
   - **Surfaces**: 땅, 벽 텍스처
   - **3D Plants**: 식물
   - **Decals**: 데칼
3. 원하는 애셋 선택
4. **Download** → 품질 선택 (4K, 8K 등)
5. **Export** → 언리얼 프로젝트 선택
6. 애셋이 자동으로 프로젝트에 추가됨

### 5.4 실습: 애셋으로 레벨 꾸미기

#### 목표
무료 애셋을 활용하여 멋진 환경을 만들어봅시다.

#### 준비물
1. Landscape Mountains (Fab에서 무료 다운로드)
2. Starter Content
3. Quixel Megascans 애셋 (선택사항)

#### 작업 순서

**1. 새 레벨 생성**
- File → New Level → Open World

**2. 지형 생성**
- Landscape Mode (Shift + 2)
- New Landscape
- Create 클릭

**3. 애셋 배치**
- Content Browser에서 다운로드한 애셋 폴더 찾기
- 나무, 바위, 풀 등을 드래그하여 배치
- 크기(R) 및 회전(E)으로 자연스럽게 배치

**4. Foliage Tool 사용**
- Foliage Mode (Shift + 4)
- + 버튼으로 Static Mesh 추가
- 풀, 나무 등을 추가
- 브러시로 드래그하여 대량 배치

**5. 조명 조정**
- Directional Light 회전으로 시간대 변경
- Sky Light Intensity 조정
- Post Process Volume 추가하여 색감 조정

**6. 머티리얼 적용**
- 바위,땅에 Megascans 머티리얼 적용
- 드래그 앤 드롭으로 간단히 적용

**7. 최종 확인**
- Play로 플레이어 시점에서 확인
- 카메라 각도 조정
- 스크린샷 촬영 (F9)

> **💡 TIP**: 
> - **Alt + P**: Pilot 모드 (뷰포트 카메라로 자유롭게 이동)
> - **G**: 게임 뷰 모드 (에디터 아이콘 숨김)
> - **F11**: 몰입 모드 (전체화면)

---

## 부록: 유용한 단축키 모음

### 일반

| 단축키 | 기능 |
|--------|------|
| **Ctrl + S** | 현재 레벨 저장 |
| **Ctrl + Shift + S** | 모두 저장 |
| **Ctrl + N** | 새 레벨 |
| **Ctrl + O** | 레벨 열기 |
| **Ctrl + P** | 프로젝트 설정 |
| **Ctrl + Space** | 콘텐츠 브라우저 열기/닫기 |
| **Ctrl + Tab** | 최근 열었던 애셋 전환 |

### 뷰포트

| 단축키 | 기능 |
|--------|------|
| **F** | 선택한 오브젝트에 포커스 |
| **Q** | 선택 모드 |
| **W** | 이동 모드 |
| **E** | 회전 모드 |
| **R** | 크기 모드 |
| **G** | 게임 뷰 모드 |
| **Alt + G** | 모든 기즈모 숨김/표시 |
| **Ctrl + D** | 복제 |
| **Del** | 삭제 |
| **End** | 오브젝트를 바닥에 붙임 |
| **Shift + 1** | Editor Mode |
| **Shift + 2** | Landscape Mode |
| **Shift + 3** | Modeling Mode |
| **Shift + 4** | Foliage Mode |
| **F9** | 스크린샷 |
| **F11** | 몰입 모드 |

### 플레이

| 단축키 | 기능 |
|--------|------|
| **Alt + P** | Play In Editor |
| **Esc** | 플레이 종료 |
| **F8** | 플레이 중 Eject (카메라 자유 이동) |
| **Pause** | 일시정지 |
| **Shift + F1** | 마우스 커서 표시 |

### 블루프린트

| 단축키 | 기능 |
|--------|------|
| **F7** | 컴파일 |
| **F9** | 브레이크포인트 |
| **F5** | 디버그 시작 |
| **우클릭** | 노드 추가 메뉴 |
| **드래그 + 우클릭** | 핀에서 노드 추가 |
| **C** | Comment 박스 추가 |
| **Alt + 클릭** | 연결 끊기 |

---

## 추가 학습 리소스

### 공식 문서
- **언리얼 엔진 공식 문서**: https://docs.unrealengine.com
- **API 레퍼런스**: https://docs.unrealengine.com/en-US/API/
- **포럼**: https://forums.unrealengine.com

### 학습 사이트
- **Unreal Online Learning**: https://dev.epicgames.com/community/learning
- **언리얼 아카데미**: 무료 온라인 강좌
- **YouTube - Unreal Sensei**: 초보자 친화적
- **YouTube - Matt Aspland**: 블루프린트 튜토리얼

### 한국어 커뮤니티
- **언리얼 엔진 한국 사용자 그룹** (Facebook)
- **언리얼 엔진 포럼 한국어 섹션**
- **디스코드 언리얼 커뮤니티**

---

